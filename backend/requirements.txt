fastapi==0.141.1
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy[asyncio]==2.0.35
aiomysql==0.3.0
pymysql==1.1.1
alembic==1.13.2

# Task queue
celery==5.4.0
redis==5.0.8

# AI / Agent orchestration
# (version cụ thể — pin sau khi verify import + LangGraph compile thành công
# trong venv thật; các bản trước đó (langchain 0.3.x) đã bị yank/lỗi thời)
langchain==1.3.17
langgraph==1.2.11
langchain-google-genai==3.2.0
langchain-openai==1.6.0

# Document parsing (Phase 1: text-based files only)
# pypdf bump 5.0.1 -> 6.15.0: bản 5.0.1 dính nhiều CVE (vòng lặp vô hạn / DoS
# khi parse PDF cố tình bị lỗi) — quan trọng vì đây là file DO NGƯỜI DÙNG UPLOAD,
# input không tin cậy.
pypdf==6.15.0
python-docx==1.1.2
openpyxl==3.1.5

# Zalo integration: KHÔNG cài lib Zalo trực tiếp ở đây. zlapi (Python) không hỗ
# trợ QR login (chỉ nhập cookie/IMEI thủ công từ trình duyệt) — không khớp UX
# "quét QR" của sản phẩm. Dùng zca-js (Node.js, có loginQR() thật) qua service
# riêng zalo-bridge/, backend gọi qua HTTP (xem app/services/zalo_service.py).

# Utils
python-multipart==0.0.31
httpx==0.27.2
