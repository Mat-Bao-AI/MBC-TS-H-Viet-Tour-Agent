# Env
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node
node_modules/
.next/
out/
*.tsbuildinfo

# Zalo session (local login state — never commit)
zalo-bridge/storage/*
!zalo-bridge/storage/.gitkeep

# Uploaded documents (local storage, phase 1)
backend/storage/uploads/

# Logs
*.log

# Local operator scripts — giữ trên máy, không phân phối qua repository.
/scripts/

# OS
.DS_Store

# MCP credentials (secret key — never commit, đã import vào ~/.claude.json user scope)
mcp_ggstitch.txt

# Công cụ nội bộ (Claude Code) — sandbox data demo + marker /checkpoint.
# KHÔNG liên quan gì tới ứng dụng, không phần nào được lên git.
_local/
.vibe/
