# /my-qc — Kết quả QC
Scope: full (ui, content, compat, perf, seo)   Thời gian: 2026-09-04   App: http://localhost:3000 (DEV, local)

Phương pháp: Playwright (Chromium) quét 9 trang yêu cầu đăng nhập + trang
công khai `/t/[id]` ở 4 breakpoint (375/768/1280/1920px) = 44 tổ hợp, bắt
console error/failed request/overflow/ảnh vỡ tự động; xem tay ảnh chụp các
trang rủi ro cao nhất; grep content toàn `frontend/src`; curl kiểm SEO tĩnh.
Bảo mật (multi-tenant, auth, secret) **không nằm trong scope này** — đã có
kế hoạch riêng ở `docs/QA-PLAN.md` mục 3, chạy qua `/my-sec`.

🟡 MEDIUM
  ✗ [`/` — danh sách tour, mobile 375px] — Hàng filter "Tất cả/Đang diễn
    ra/Sắp tới/Đã hoàn thành" bị cắt cứng ở lề phải màn hình, không có gợi ý
    trực quan (gradient/peek) báo còn nội dung cuộn được. Đã xác nhận kỹ
    thuật vẫn cuộn ngang được (`overflow-x-auto`, `scrollWidth 377 > clientWidth
    343`) — không phải bug chặn chức năng, chỉ khó nhận biết được là còn tab
    "Đã hoàn thành" nằm ngoài màn hình.
    → Gợi ý: thêm gradient mờ bên phải hàng pill hoặc padding-right đủ để hé
    lộ 1 phần chữ tab kế tiếp — pattern quen thuộc cho scrollable tab list.
    → Ảnh: `docs/qc-evidence-2026-09-04/tour-list-filter-overflow-mobile.png`
    → File: `frontend/src/app/page.tsx:66`

  ✗ [`/settings` — Kết nối Zalo cá nhân] — Khi mất kết nối Zalo, dòng lý do
    hiện nguyên văn tiếng Anh kỹ thuật từ thư viện `zca-js`: "Cannot get scan
    result" — lạc tông giữa 1 UI toàn tiếng Việt, không giúp HDV hiểu cần
    làm gì tiếp.
    → Gợi ý: bọc lại thành thông báo thân thiện, vd "Không lấy được kết quả
    quét QR — thử quét lại." thay vì hiện thẳng `status.error` từ backend.
    → Ảnh: `docs/qc-evidence-2026-09-04/zalo-status-raw-error-mobile.png`
    → File: `frontend/src/app/settings/page.tsx:102` (`{status.error}`)

✓ Đã kiểm và OK:
  - 44/44 tổ hợp trang×breakpoint: 0 horizontal-overflow bất thường, 0 ảnh vỡ, 0 console error, 0 failed request (ngoại trừ 1 tour test đã tự dọn — xem ghi chú dưới).
  - Trang công khai `/t/[id]`: render đúng cả 4 breakpoint, tiêu đề tab đúng `{tên tour} — Lịch trình tour`, footer/logo hiện đúng tên công ty đã cấu hình (white-label hoạt động đúng, kiểm độc lập với phiên sửa trước đó).
  - Onboarding tour (react-joyride) tự bật đúng lúc vào Dashboard lần đầu, hiển thị đúng tên công ty trong lời chào.
  - Nội dung: không tìm thấy Lorem ipsum/placeholder/TODO/FIXME còn sót trong `frontend/src`.
  - SEO tĩnh: `/signin` có đúng 1 `<h1>`, `<title>`/`<meta description>` không rỗng. `sitemap.xml`/`robots.txt` không có — hợp lý vì đây là app nội bộ (đăng nhập mới dùng được), không flag.
  - Perf smoke: `/signin` load ~167ms, `/t/[id]` load ~81ms — rất nhanh, dưới xa ngưỡng 3s.
  - Compat: chỉ tự động test được Chromium (giới hạn Playwright mặc định) — **chưa test tay** Firefox/Safari/Edge/mobile thật, xem checklist bên dưới.

⚠️ Ghi chú phát sinh ngoài scope (không phải bug — thông tin để bạn biết):
  - Lúc quét, phát hiện bảng `tours` trong DB đang **rỗng** (tour thật
    `Lich_Trinh_Du_Lich_Da_Lat.xlsx` đã sửa lỗi Gemini trước đó không còn
    trong hệ thống nữa — có thể bạn đã tự xoá qua nút "Xoá tour"). Đã tạo 1
    tour test tạm để QC trang công khai, **đã xoá sạch sau khi xong**, không
    còn dữ liệu test nào sót lại.

☐ Compat — checklist test tay (chưa làm, cần bạn hoặc 1 lượt test riêng):
  [ ] Chrome desktop     [ ] Chrome Android
  [ ] Firefox desktop
  [ ] Edge desktop
  [ ] Safari desktop     [ ] Safari iOS
  [ ] Scroll mượt trên mobile thật    [ ] Tap không delay lạ

─────────────────────────────────────────
Tổng: 0 critical, 0 high, 2 medium, 0 low

## Kết quả xử lý (`/my-qf`) — 2026-09-04

Cả 2 finding thuộc Nhóm A (chỉ sửa CSS/wording, không đụng auth/schema, 1 cách
sửa hợp lý duy nhất) — tự sửa luôn, không cần hỏi.

- ✓ Fixed: filter pills bị cắt cứng mobile — thêm gradient fade bên phải hàng
  pill làm tín hiệu còn nội dung cuộn được (`frontend/src/app/page.tsx:66`).
  Verify: `.bg-gradient-to-l` đã render, hàng pill vẫn cuộn được đúng
  (`scrollWidth 377 > clientWidth 343`).
- ✓ Fixed: thông báo lỗi Zalo tiếng Anh kỹ thuật — dịch thành "Không lấy được
  kết quả quét QR — thử quét lại.", giữ lỗi gốc ở `title` để debug
  (`frontend/src/app/settings/page.tsx:104`). Verify: text tiếng Anh gốc
  không còn hiện trên UI, text tiếng Việt mới đã hiện đúng.

Đã build + redeploy `frontend`, verify lại bằng Playwright (scope `ui`, không
cần chạy lại `full`) — cả 2 fix pass, không phát sinh regression mới.
